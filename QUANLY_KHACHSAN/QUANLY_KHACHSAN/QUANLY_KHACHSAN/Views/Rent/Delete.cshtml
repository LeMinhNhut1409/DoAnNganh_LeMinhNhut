@model QUANLY_KHACHSAN.Models.Phieuthue

@{
    ViewData["Title"] = "Xoá phiếu thuê";
    // Kiểm tra vai trò từ claims
    var role = User.FindFirst("Role")?.Value;

    // Đặt layout dựa trên vai trò
    if (role == "Staff")
    {
        Layout = "_StaffLeTanLayout"; // Đặt layout cho nhân viên
    }
    else if (role == "Manager")
    {
        Layout = "_ManagerLayout"; // Đặt layout cho quản lý
    }
    else
    {
        Layout = "_DefaultLayout"; // Layout mặc định nếu không xác định được vai trò
    }
       }

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Phieuthue</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Ngaylappt)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Ngaylappt)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.MakhNavigation)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.MakhNavigation.Makh)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.MapNavigation)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.MapNavigation.Tenphong)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Mapt" />
        <button type="submit" class="btn btn-danger">Xóa</button>  |
        <a asp-action="Index">Quay lại danh sách</a>

    </form>
</div>
